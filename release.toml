# Edit this file on each release. Run `pebble build` (or `node scripts/generate-release.js`)
# to sync package.json and generate src/pkjs/release.js for the update notice.

version = "1.2.0"

message = """
Public holidays are now available on the calendar. 
Weather chart chaching and persistance were improved.

Thanks for your patronage <3
"""
